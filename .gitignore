__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# basic-auth хэш (не нужен — auth снят)
infra/trade.htpasswd

# деплой-конфиги с внутренними адресами (не публикуем)
infra/

# рантайм-состояние сессии (не коммитим)
session_state.json
session_state_*.json

# результаты прогонов и live-состояние momentum (рантайм-артефакты)
pairsignal/out/

# служебные файлы Claude
.claude/
